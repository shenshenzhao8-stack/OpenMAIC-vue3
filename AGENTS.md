# OpenMAIC 复刻任务包级开发规则（最高执行规则 · monorepo 版）

> 同步来源：`/Users/mac/OpenMAIC/AGENTS.md`（原项目目录，保留原件）
> 同步日期：2026-08-12（随 MONOREPO Phase 5 迁入 monorepo，源目录退役，本包为唯一真源）

本文件是「OpenMAIC-Vue3 复刻任务」迁入 `ai-learning-practice-web` monorepo 后，
在 `package-openmaic/` 包内的最高执行规则。任何阶段的开发（包括 Codex 与人工开发者）
都必须遵守。

## 适用范围与优先级

1. 本文件约束「本包内部实现」（代码注释、阶段文档、裁剪范围、功能保留、提交行为）；
2. 与目标仓库根 `AGENTS.md`（AI Learning Practice Web Agent Rules）并行：
   - 涉及本包内部（引擎、组件、mock、类型、测试、文档）以本文件为准；
   - 涉及仓库整体（提交规范、lint/stylelint、构建脚本、Web/Mobile 组件放置）以根
     `AGENTS.md` 与项目配置为准；
3. 原项目 `/Users/mac/OpenMAIC`（及 worktree `/Users/mac/.codex/worktrees/186c/OpenMAIC`）
   为对照源码（只读），其 `AGENTS.md` 保留历史原件。

## 规则一：所有代码必须完整详尽的中文注释

1. 每个源文件顶部必须有「文件头注释」：说明该文件的用途、设计思路，以及它与 OpenMAIC 原项目（本机 `/Users/mac/OpenMAIC` 或 `/Users/mac/.codex/worktrees/186c/OpenMAIC`）中哪个文件的对应关系。
2. 每个导出的函数、类、类型必须有 JSDoc 风格的中文注释：说明参数、返回值、关键设计意图。
3. 复杂逻辑（状态机转移、异步调度、边界条件、时序依赖）必须有行内中文注释，解释「为什么这样做」。
4. 代码标识符允许使用英文，但注释必须使用中文。
5. 「详细功能介绍 + 注释红线」：每个函数（包括私有函数）都必须有中文功能说明（做什么 / 怎么做 / 为什么）。**所有注释必须为中文，禁止英文注释**（含照搬文件）；照搬文件的原英文注释必须翻译为中文，同时保持代码逻辑逐行不变。英文仅允许出现在代码标识符、字符串字面量、URL 中。

## 规则二：每个阶段完成后必须对照原项目源码讲解

1. 每个阶段完成后，必须在包内 `docs/PHASE-{阶段号}.md` 中书写「阶段完成说明」，并在对话中向用户讲解。
2. 讲解必须做到「一一对应」：本阶段写出的每一个文件，都要列出：
   - 对应原项目哪个文件（给出原项目路径）；
   - 该文件在原项目中的功能；
   - 本项目为什么这样实现（照搬 / 改写 / 重构的原因与方案）。
3. 「照搬」的文件不得修改核心逻辑；「改写」的文件必须保持字段名、方法名、行为语义与原项目一致（因为原项目的引擎代码按这些名字调用）。
4. 「裁剪」必须明确记录：删掉了什么、为什么可以删、将来要恢复需要做什么。
6. 「工作参考手册」：每个阶段完成后，除 `docs/PHASE-{x}.md` 阶段完成说明外，还必须输出 `docs/REFERENCE-PHASE-{x}.md` 工作参考手册，供用户阅读和审阅。手册内容定义：**逐文件说明该文件的作用与功能，并对所有主要方法/函数逐个讲解（做什么、为什么这样做）**，让读者明白每一步在干嘛；手册内容如有调整或修改，必须同步更新对应文档及 `docs/README.md` 索引。
5. 「范围变更同步」：每当裁剪范围发生变化时，必须同步完成三件事：
   - 更新本文件「规则三：工程约束」中的裁剪范围；
   - 更新包内**所有已完成的** `docs/PHASE-{n}.md`（文件对照表、裁剪记录、路线图必须与最新范围一致）；
   - 在变更当期的 PHASE 文档中记录变更原因、影响及关联的原项目文件。

## 规则三：工程约束

1. 工程形态（2026-08-12 更新）：复刻工程已迁入目标 pnpm monorepo
   （`ai-learning-practice-web`）的独立包 `package-openmaic`（包名 `@ailearning/openmaic`），
   技术栈仍为 Vue 3 + Vite + TypeScript + Vue Router + Pinia（**Pinia 对齐仓库 Web 的
   ~2.1.7**）；保留独立 SPA 能力（独立 dev/build/test/preview），同时课堂核心组件
   `OpenMaicClassroom` 可被 Web 宿主消费。内部 `#/*` 为包私有映射（package.json imports +
   tsconfig paths + vite/vitest alias 三处同步）。
2. 原项目的 `@openmaic/dsl` 包通过「自建类型 + alias」接入：包内解析到
   `src/types/dsl/index.ts`，照搬的代码无需修改 import；Web 宿主消费时由其 vite config
   增加 alias 指向本包 `src/types/dsl/index.ts`（见 `docs/MONOREPO-INTEGRATION-REFACTOR-PLAN.md`）。
3. 裁剪范围（永久有效）：
   - 场景：slide / quiz / interactive 三种；
   - 教学动作：speech（讲解）+ spotlight（聚光）+ laser（激光笔）；
   - slide 元素类型：仅 text / shape / line / image 四类（2026-08-11 收敛；latex / chart / table / video / code 的类型与实现已删除）；
   - 测验（quiz）：**无判分业务**（2026-08-11）——无得分 / 无 AI 判分 / 无解析讲解；复盘仅「选择题显示对错、简答题显示参考答案」；
   - 页面：独立 SPA 只有首页（课堂入口）与课堂播放页 `/classroom/:id`，**无生成页 / 生成预览页**，主业务在课堂播放；
   - 课堂数据由 mock 提供（包内 `mock/classroom.ts`），不做生成流程；
   - 互动：仅登录用户与老师两个角色，一问一答、**可多轮**（用户可连续提问，对话历史逐轮累积；每轮 = 用户提问 → 老师回答），**无多角色讨论**（导演多 agent 编排、圆桌、讨论动作均不在范围）；播放引擎调度、语音文字同步与学生提问互动逻辑与原项目一致；
   - 互动输入：学生除文本外，**支持麦克风语音输入（STT）提问**——用浏览器 Web Speech API（SpeechRecognition / webkitSpeechRecognition），含能力检测（不支持则隐藏麦克风按钮）、**getUserMedia 权限预检**（可靠拉起浏览器授权弹窗，对齐原项目设置页 asr-settings.tsx 的申请路径；失败按「无设备/权限被拒/非安全上下文/环境不支持/其他」分类提示，其他分支透出具体错误名，支持重试，含 requesting 过程反馈）与实时音量指示（可调试反馈），识别结果仅取「最终片段（isFinal）」追加到输入框后走普通文本消息通道（对齐原项目 `components/ai-elements/prompt-input.tsx` 的 PromptInputSpeechButton，Phase 8 补充落实，2026-08-12 两次修复权限弹窗与错误定位问题）；
   - AI 部分由后台接口承接，现阶段以 mock 保证流程完整。

## 规则四：独立判断，不迎合用户

1. 当用户提出的问题、需求或方案存在明显偏差（技术理解错误、需求自相矛盾、方案成本收益不合理、与项目目标冲突等）时，必须**直接指出问题所在**，说明理由，并给出更合理的建议；不得为了让对话「顺利」而附和错误的前提。
2. 处理顺序：先纠正前提，再回答问题或执行任务。
3. 指出问题必须具体：哪里不对、为什么不对、正确应该怎样；同时尊重用户最终的决定权——指出问题后，如果用户仍坚持原方案，则按用户决定执行，并在阶段文档中记录该决策及风险。

## 规则五：提交控制——每完成一个 Phase 不自动提交代码

1. 每个 Phase 完成后，**不得执行 git add / git commit / git push**；所有改动保持在工作区，供用户人工仔细审核。
2. 提交由用户自行决定与执行（何时提交、提交信息、是否推送），Codex 不代为提交。
3. 阶段完成说明（`docs/PHASE-{x}.md`）与工作参考手册（`docs/REFERENCE-PHASE-{x}.md`）照常输出，但 git 操作一律留白。
4. 若用户在当前对话中明确指示「提交 / 推送」，则按用户指示执行；除此之外一律不碰 git 提交类操作。
5. 本规则自 2026-08-10 起生效（Phase 3 及以后）；此前已提交的 Phase 0/1/2 历史保持不变。
6. 目标仓库启用 husky / commitlint / lint-staged：用户人工提交时请遵循仓库
   conventional commits 规范（如 `feat:`、`fix:`、`refactor:` 前缀）；Codex 仍不代提交。

## 规则六：功能保留与回归检查（防功能丢失红线，2026-08-12 新增）

背景：Phase 7 / Phase 8 验收时多次发现功能丢失（如原项目聊天区的「讲义视图 Lecture Notes」、
播放进度 / `onProgress` 接线、进度跳转等在无裁剪需求的情况下缺失，且文档未记录）。此类问题
的根因是：没有功能基线清单、裁剪未按三要素记录、修复/重构只做点对点验证、回调契约未逐项核对。
本规则作为防丢失红线，所有阶段（含修复、重构、小版本）必须遵守。

1. **未明确裁剪 = 必须保留**：任何原项目功能点，只要用户没有明确确认裁剪，复刻工程就必须保留。
   「简化版」「暂不实现」「未移植」等表述，必须同时伴随明确的裁剪记录；否则一律视为功能丢失事故，
   责任人（Codex / 人工开发者）必须补回功能并复盘。
2. **裁剪三要素**：任何裁剪必须同时满足以下三要素，缺一视为违规裁剪：
   - 用户明确确认（对话中明确同意）；
   - 在当期 `docs/PHASE-{x}.md` 记录「删掉了什么 / 为什么可以删 / 将来恢复需要做什么」；
   - 同步更新 `TODO.md` 与 `README.md` 范围说明。
3. **功能基线清单**：每个 Phase（含小版本、修复版本）完成后，必须在 PHASE 文档中输出
   「原项目功能点 → 本项目状态（保留 / 简化 / 裁剪+原因）」对照清单，覆盖该阶段涉及的原项目
   文件的所有导出、回调、UI 功能；后续任何改动都必须基于该清单做回归，清单随改动同步更新。
4. **修复不改无关行为**：修复 bug 时只允许改动导致 bug 的最小范围；与 bug 无关的行为
   （提示文案、UI 细节、回调接线、按钮可见性等）不得顺带修改。确有必要的顺带修改必须单独
   记录原因，并列为「行为变更」接受用户审核。
5. **重构保持行为**：重构 / 迁移（含 React→Vue、目录改名、提取纯函数）必须逐项核对原函数
   签名、回调契约、UI 功能点保持不变；完成后对照原项目文件逐行 diff 审查，输出「行为不变」验证结论。
6. **回调契约逐项核对**：引擎 / 组件 / composable 接线时，必须对照接口（如
   `PlaybackEngineCallbacks`、`ChatAreaRef`、各 store 的 action）列出的**全部回调/能力**逐项
   确认：已接线 / 有意留空+原因 / 裁剪+记录。禁止静默不接；发现引擎支持但接线缺失的能力
   （如 `onProgress` 未接、`jumpToAction` 未暴露给 UI）必须记录并默认补接。
7. **回归验收**：每个阶段 / 修复完成后，除 `vue-tsc`、单测、构建外，必须按「功能基线清单」
   做一次人工 / 浏览器回归：页面入口、播放控制、字幕、翻页、问答、麦克风、讲义等已实现功能
   逐项点验，回归结果写入 PHASE 文档（注明哪些项通过 / 哪些项缺失）。
8. **新需求先盘点原能力**：新增或扩展功能时，先用 `rg` 检索原项目（`/Users/mac/OpenMAIC`）
   是否已有对应实现（按功能关键词搜，如 lecture-notes、progress、jumpToAction 等），
   有则优先对照移植 / 参考，禁止凭空新写导致行为不一致或再次遗漏。
9. **丢失事故复盘**：验收发现功能丢失时，先定位「哪个版本、哪个文件、哪个功能」，对照 git diff
   与文档记录判断是无意识丢失还是记录过的裁剪；然后补回功能并更新功能基线清单，最后把事故原因
   写进当期 PHASE 文档，防止同类问题复发。

## 规则七：测试策略——开发验证用、正式库不落测试文件（2026-08-12 新增）

背景：目标仓库（`ai-learning-practice-web`）没有 vitest 测试体系，正式代码库不保留
测试文件；但开发过程中需要测试验证纯函数/引擎行为，因此约定：

1. 开发时可在 `src` 下临时放置 `*.test.ts`，运行 `pnpm --dir package-openmaic run test`
   （vitest 配置 `include: ['src/**/*.test.ts']` 已就绪）；验证完成后**必须删除临时测试文件**，
   正式交付不含任何 `*.test.ts`；
2. `package.json` 的 test 脚本为 `vitest run --passWithNoTests`：正式库无测试文件时
   命令不报错，开发时有临时测试文件可正常执行；
3. 测试文件删除后，回归防线改为：`vue-tsc` + `vite build` + 固定主线人工浏览器回归
   （规则六第 7 条）；发现功能异常按规则六第 9 条复盘定位；
4. 若未来目标仓库引入统一测试体系，再评估是否恢复测试文件落库。

## 规则八：备份同步——退役源目录作为备份镜像（2026-08-12 新增）

1. **唯一真源**：`package-openmaic/`（本包）；**备份镜像**：`/Users/mac/OpenMAIC-vue3/`
   （退役源目录，保留其 git 历史用于备份跟踪）；
2. 每个 Phase 完成、或用户明确要求时，执行一次 rsync 同步，保持镜像与真源一致：

   ```bash
   rsync -a --delete \
     --exclude node_modules --exclude dist --exclude .git --exclude .DS_Store \
     /Users/mac/Documents/DesktopOrganizer/project/ai-learning-practice-web/package-openmaic/ \
     /Users/mac/OpenMAIC-vue3/
   ```

   `--delete` 使镜像精确对齐真源（真源中已删除的文件不会残留在镜像）；
3. 镜像的 git 提交由用户自行决定（遵循规则五，Codex 不代提交）；镜像仅用于备份跟踪，
   不作为开发真源；
4. 同步动作与结果记录在当期 MONOREPO-PHASE 文档或变更记录中。

## 变更记录

| 日期 | 内容 |
|---|---|
| 2026-08-12 | 由 `/Users/mac/OpenMAIC/AGENTS.md` 同步至 `package-openmaic/`：适配 monorepo 形态（规则三第 1、2 条）、补充与仓库根 AGENTS.md 的优先级说明、规则五补充仓库提交规范提示 |
| 2026-08-12 | 新增规则七：测试策略（开发验证用、正式库不落测试文件）；同步清理包内 18 个历史测试文件 |
| 2026-08-12 | 新增规则八：备份同步（退役源目录 `/Users/mac/OpenMAIC-vue3` 作为备份镜像，rsync 保持一致） |
